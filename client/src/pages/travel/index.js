import React from 'react';
import MapContainer from '../../components/map';

const Travel = () => {
    return (
        <div>
           <MapContainer />
        </div>
    );
};

export default Travel;
